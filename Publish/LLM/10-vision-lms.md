- Generating images using Stable Diffusion
	- text 2 image generation
	- principle:
		- first destroy then restore/ reconstruct
		- learning process: first add noises till it becomes a completely blurry image  and then the model learns to remove the noises 
		- so giving a prompt, and then the model generates a blurry noisy image and then removing them and get a clear image
# Qwen 2.5VL for bounding - box segmentation
- 